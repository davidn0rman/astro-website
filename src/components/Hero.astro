---
export interface Props {
	title: string;
	body: string;
	href: string;
}
---

<!-- <section class="w-dvw h-dvh flex items-center justify-center flex-col bg-gradient-to-tr from-your-darkness to-vivid-sky-blue"> -->
<section class="w-dvw h-dvh flex items-center justify-center flex-col bg-radialGradient">
	<div>
		<h1 class="flex-1 content-center text-white custom-h1">Hello, my name is david</h1>
		<p class="text-white">Here we go again.</p>
	</div>
</section>

<style>
	.custom-h1 {
		font-size: clamp(2rem, 6vw, 8rem);
	}
	/* .radial-gradient {
		background: radial-gradient(circle, #00c8ff, #0094d8, #2162ab, #2e3277, #280041);
	} */
</style>
